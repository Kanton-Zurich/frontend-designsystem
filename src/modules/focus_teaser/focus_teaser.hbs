<div class="mdl-focus_teaser">
  {{#if focusTeaserHeader }}
  <h2
    {{#if anchorNavReference}}id="{{anchorNavReference}}" data-anchornav="target"{{/if}}
    class="atm-heading atm-heading--bordered"
  >{{focusTeaserHeader.title}}</h2>
  <p class="atm-paragraph">
    {{focusTeaserHeader.text}}
  </p>
  {{/if}}
  {{#each items}}
    {{> "modules/focus_card/focus_card" this}}
  {{/each}}
</div>
